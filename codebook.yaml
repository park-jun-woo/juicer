required:
  feature:
    scan: "OpenAPI endpoint extraction from Go+Gin code"
    ddl: "DDL schema extraction from migration files"
    sql: "SQL skeleton extraction from repository code"
    hurl: "Hurl endpoint test ratchet loop"
    ratchet: "sequential verification loop for sqlc query writing"
    prisma: "Prisma schema parsing to DDL tables"
  type:
    command: "CLI entrypoint and subcommand dispatch"
    extract: "static analysis extraction logic"
    parse: "source code or SQL text parsing"
    model: "data structure definition"
    render: "output formatting and serialization"
    session: "ratchet session state management"
    convert: "data transformation between formats"
    test: "unit test"
